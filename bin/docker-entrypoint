#!/bin/bash -e

# If running any Rails command, ensure database exists and is migrated.
if [ "$1" = "./bin/rails" ] || [ "$1" = "bin/rails" ] || [ "$1" = "rails" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
