#!/usr/bin/env bash
set -e

if [ "${RAILS_ENV:-development}" = "production" ]; then
  ./bin/rails db:prepare
elif [ "$1" = "./bin/rails" ] || [ "$1" = "bin/rails" ] || [ "$1" = "rails" ]; then
  ./bin/rails db:prepare
fi

exec "$@"
